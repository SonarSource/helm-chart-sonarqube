{{- /*
Dedicated ServiceAccounts for the agentic workloads: one for the orchestrator and one per enabled
runtime family. Each is self-contained (create/name/automountToken/annotations), independent of the
other components, so each pod can bind its own cloud identity (e.g. an AWS IAM role via IRSA). Names
are resolved by the helpers so an external IAM trust policy can pin exact k8s subjects.
*/}}
{{- if and .Values.orchestrator.enabled .Values.orchestrator.serviceAccount.create }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sonarqube.agentic.orchestrator.serviceAccountName" . }}
  labels:
    app: {{ template "sonarqube.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    sonarqube.agentic/component: orchestrator
  {{- with .Values.orchestrator.serviceAccount.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.orchestrator.serviceAccount.automountToken }}
{{- end }}
{{- range $family, $cfg := fromYaml (include "sonarqube.agentic.runtimes" .) }}
{{- if and $cfg.enabled $cfg.serviceAccount.create }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sonarqube.agentic.runtime.serviceAccountName" (dict "ctx" $ "family" $family) }}
  labels:
    app: {{ template "sonarqube.name" $ }}
    chart: {{ $.Chart.Name }}-{{ $.Chart.Version | replace "+" "_" }}
    release: {{ $.Release.Name }}
    heritage: {{ $.Release.Service }}
    sonarqube.agentic/component: runtime
    sonarqube.agentic/family: {{ $family }}
  {{- with $cfg.serviceAccount.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
  {{- end }}
automountServiceAccountToken: {{ $cfg.serviceAccount.automountToken }}
{{- end }}
{{- end }}
