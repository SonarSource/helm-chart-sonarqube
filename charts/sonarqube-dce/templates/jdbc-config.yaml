apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sonarqube.fullname" . }}-jdbc-config
  labels: {{- include "sonarqube.labels" . | nindent 4 }}
data:
  SONAR_JDBC_USERNAME: {{ include "jdbc.username" . }}
  {{- if .Values.jdbcOverwrite.enable }}
  SONAR_JDBC_URL: {{ .Values.jdbcOverwrite.jdbcUrl | trim | quote }}
  {{- else if and .Values.postgresql.service.port .Values.postgresql.postgresqlDatabase }}
  SONAR_JDBC_URL: "jdbc:postgresql://{{ include "postgresql.hostname" . }}:{{- .Values.postgresql.service.port -}}/{{- .Values.postgresql.postgresqlDatabase -}}"
  {{- end -}}
