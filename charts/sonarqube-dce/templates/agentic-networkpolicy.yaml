{{- if and .Values.networkPolicy.enabled .Values.orchestrator.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sonarqube.agentic.orchestrator.fullname" . }}
  labels:
    app: {{ template "sonarqube.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    sonarqube.agentic/component: orchestrator
spec:
  podSelector:
    matchLabels:
      app: {{ template "sonarqube.name" . }}
      sonarqube.agentic/component: orchestrator
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: {{ template "sonarqube.name" . }}
              sonarqube.datacenter/type: app
      ports:
        - port: {{ .Values.orchestrator.port }}
          protocol: TCP
    - from:
        - podSelector:
            matchLabels:
              app: {{ template "sonarqube.name" . }}
              sonarqube.agentic/component: runtime
      ports:
        - port: {{ .Values.orchestrator.port }}
          protocol: TCP
  egress:
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    {{- range $family, $cfg := fromYaml (include "sonarqube.agentic.runtimes" .) }}
    {{- if $cfg.enabled }}
    - to:
        - podSelector:
            matchLabels:
              app: {{ template "sonarqube.name" $ }}
              sonarqube.agentic/component: runtime
              sonarqube.agentic/family: {{ $family }}
      ports:
        - port: {{ $cfg.port }}
          protocol: TCP
    {{- end }}
    {{- end }}
    - to:
        - podSelector:
            matchLabels:
              app: {{ template "sonarqube.name" . }}
              sonarqube.datacenter/type: app
      ports:
        - port: 9000
          protocol: TCP
    {{- range .Values.orchestrator.egressAllow }}
    - to:
{{ include "sonarqube.agentic.egressAllow.peer" . | indent 8 }}
      {{- with .ports }}
      ports:
        {{- range . }}
        - port: {{ .port }}
          {{- with .protocol }}
          protocol: {{ . }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- end }}
{{- end }}

{{- /* Runtime NetworkPolicies render on the global networkPolicy.enabled OR each runtime's own
       dedicated networkPolicy.enabled, so the (untrusted) runtimes can be locked down on their own
       without imposing a NetworkPolicy on the orchestrator or the SonarQube app/search nodes. */}}
{{- range $family, $cfg := fromYaml (include "sonarqube.agentic.runtimes" .) }}
{{- if and $cfg.enabled (or $.Values.networkPolicy.enabled $cfg.networkPolicy.enabled) }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sonarqube.agentic.runtime.fullname" (dict "ctx" $ "family" $family) }}
  labels:
    app: {{ template "sonarqube.name" $ }}
    chart: {{ $.Chart.Name }}-{{ $.Chart.Version | replace "+" "_" }}
    release: {{ $.Release.Name }}
    heritage: {{ $.Release.Service }}
    sonarqube.agentic/component: runtime
    sonarqube.agentic/family: {{ $family }}
spec:
  podSelector:
    matchLabels:
      app: {{ template "sonarqube.name" $ }}
      sonarqube.agentic/component: runtime
      sonarqube.agentic/family: {{ $family }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: {{ template "sonarqube.name" $ }}
              sonarqube.agentic/component: orchestrator
      ports:
        - port: {{ $cfg.port }}
          protocol: TCP
  egress:
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    - to:
        - podSelector:
            matchLabels:
              app: {{ template "sonarqube.name" $ }}
              sonarqube.agentic/component: orchestrator
      ports:
        - port: {{ $.Values.orchestrator.port }}
          protocol: TCP
    {{- /* The runtime reads/writes job artifacts directly against object storage via presigned URLs,
           bypassing the orchestrator, so it needs its own egress to reach it - the chart has no way
           to resolve orchestrator.storage to a NetworkPolicy peer, so egressAllow must cover it
           (see the comment on hunterAgent/remediationAgent.egressAllow in values.yaml). */}}
    {{- range $cfg.egressAllow }}
    - to:
{{ include "sonarqube.agentic.egressAllow.peer" . | indent 8 }}
      {{- with .ports }}
      ports:
        {{- range . }}
        - port: {{ .port }}
          {{- with .protocol }}
          protocol: {{ . }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- end }}
{{- end }}
{{- end }}
