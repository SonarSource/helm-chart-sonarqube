# Default values for SonarQube.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# (DEPRECATED) If the deployment Type is set to Deployment SonarQube is deployed as a replica set.
# StatefulSet will be removed in a future release in favor of Deployment. Default to StatefulSet for backwards compatibility.
deploymentType: "StatefulSet"

# There should not be more than 1 SonarQube instance connected to the same database. Please set this value to 1 or 0 (in case you need to scale down programmatically).
replicaCount: 1

# How many revisions to retain (Deployment ReplicaSets or StatefulSets)
revisionHistoryLimit: 10

# (DEPRECATED) This will use the default deployment strategy unless it is overridden
deploymentStrategy:
  type: Recreate
# Uncomment this to scheduler pods on priority
# priorityClassName: "high-priority"

## Use an alternate scheduler, e.g. "stork".
## ref: https://kubernetes.io/docs/tasks/administer-cluster/configure-multiple-schedulers/
##
# schedulerName:

## OpenShift specific configuration
OpenShift:
  enabled: false
  # (Deprecated) this parameter should not be needed anymore, we support Openshift SCCv2 by default when Openshift.enabled=true
  createSCC: false
  route:
    enabled: false
    host: "sonarqube.your-org.com"
    path: "/"
    # Add tls section to secure traffic.
    tls:
      termination: edge
      # certificate:
      # key:
      # caCertificate:
      # insecureEdgeTerminationPolicy: Redirect
    wildcardPolicy: None
    annotations: {}
    # See Openshift/OKD route annotation
    # https://docs.openshift.com/container-platform/4.10/networking/routes/route-configuration.html#nw-route-specific-annotations_route-configuration
    # haproxy.router.openshift.io/timeout: 1m
    # Additional labels for Route manifest file
    # labels:
    #  external: 'true'

# Configure the edition of SonarQube Server to deploy: developer or enterprise
# edition: ""

# Set the chart to use the latest released SonarQube Community Build
community:
  enabled: false
  buildNumber: "26.7.0.124771"

# The following settings are used when deploying to Azure Marketplace
# global:
#   azure:
#     images:
#       sonarqube:
#         registry: registry-test
#         image: sonarqube
#         tag: 2025.2.0-enterprise

image:
  repository: sonarqube
  # tag: ""
  pullPolicy: IfNotPresent
  # If using a private repository, the imagePullSecrets to use
  # pullSecrets:
  #   - name: my-repo-secret

# Set security context for sonarqube pod.
# The current section contains the default values set in a generic Kubernetes cluster. If you are using OpenShift, you should not set any specific fsGroup.
securityContext:
  fsGroup: 0

# Set security context for sonarqube container.
# The current section contains the default values set in a generic Kubernetes cluster. If you are using OpenShift, you should not set any specific UID or GID to be used for the execution.
containerSecurityContext:
  # Sonarqube dockerfile creates sonarqube user as UID and GID 0
  # Those default are used to match pod security standard restricted as least privileged approach
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 0
  seccompProfile:
    type: RuntimeDefault
  capabilities:
    drop: ["ALL"]
#   readOnlyRootFilesystem: true

# Settings to configure elasticsearch host requirements
elasticsearch:
  # (DEPRECATED) Use initSysctl.enabled instead
  configureNode: false
  bootstrapChecks: true

service:
  type: ClusterIP
  externalPort: 9000
  internalPort: 9000
  labels:
  annotations: {}
  # May be used in example for internal load balancing in GCP:
  # cloud.google.com/load-balancer-type: Internal
  # loadBalancerSourceRanges:
  #   - 0.0.0.0/0
  # loadBalancerIP: 1.2.3.4

# Those proxy settings will be propagated to the install-plugin and prometheus-exporter init containers
# if httpProxySecret is set the other one will be ignored
# the secret should contain exactly the keys http_proxy, https_proxy and no_proxy
httpProxySecret: ""
httpProxy: ""
httpsProxy: ""
noProxy: ""

# Optionally create Network Policies
networkPolicy:
  enabled: false

  # If you plan on using the jmx exporter, you need to define where the traffic is coming from
  prometheusNamespace: "monitoring"

  # If you are using a external database and enable network Policies to be created
  # you will need to explicitly allow egress traffic to your database
  # (DEPRECATED) please use additionalNetworkPolicies instead
  # additionalNetworkPolicys:
  # expects https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.21/#networkpolicyspec-v1-networking-k8s-io
  # additionalNetworkPolicies:

# will be used as default for ingress/httproute path and probes path, will be injected in .Values.env as SONAR_WEB_CONTEXT
# if .Values.env.SONAR_WEB_CONTEXT is set, this value will be ignored
sonarWebContext: ""

httproute:
  enabled: false
  # gateway: my-gateway
  # gatewayNamespace: my-gateway-namespace # optional
  # labels:
  #   somelabel: somevalue
  # hostnames:
  #   - sonarqube.your-org.com
  # The rules are optional, by default we will create one with the SonarWebContext prefix and the SonarQube service values
  # rules:
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /bar
  #   backendRefs:
  #   - name: my-service1
  #     port: 8080

ingress:
  enabled: false
  # Used to create an Ingress record.
  hosts:
    - name: sonarqube.your-org.com
      # Different clouds or configurations might need /* as the default path
      # path: /
      # For additional control over serviceName and servicePort
      # serviceName: someService
      # servicePort: somePort
      # the pathType can be one of the following values: Exact|Prefix|ImplementationSpecific(default)
      # pathType: ImplementationSpecific
  annotations: {}
  # kubernetes.io/tls-acme: "true"

  # Set the ingressClassName on the ingress record. Required, since this chart
  # no longer bundles an ingress-nginx controller to default it from.
  # ingressClassName: nginx

  # Additional labels for Ingress manifest file
  # labels:
  #  traffic-type: external
  #  traffic-type: internal
  tls: []
  # Secrets must be manually created in the namespace. To generate a self-signed certificate (and private key) and then create the secret in the cluster please refer to your ingress controller's documentation.
  # - secretName: chart-example-tls
  #   hosts:
  #     - chart-example.local

# Affinity for pod assignment
# Ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#affinity-and-anti-affinity
affinity: {}

# Tolerations for pod assignment
# Ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
# taint a node with the following command to mark it as not schedulable for new pods
# kubectl taint nodes <node> sonarqube=true:NoSchedule
# The following statement will tolerate this taint and as such reverse a node for sonarqube
tolerations: []
#  - key: "sonarqube"
#    operator: "Equal"
#    value: "true"
#    effect: "NoSchedule"

# Node labels for pod assignment
# Ref: https://kubernetes.io/docs/user-guide/node-selection/
# add a label to a node with the following command
# kubectl label node <node> sonarqube=true
nodeSelector: {}
#  sonarqube: "true"

# hostAliases allows the modification of the hosts file inside a container
hostAliases: []
# - ip: "192.168.1.10"
#   hostnames:
#   - "example.com"
#   - "www.example.com"

readinessProbe:
  exec:
    command:
      - sh
      - -c
      - |
        #!/bin/bash
        # A Sonarqube container is considered ready if the status is UP, DB_MIGRATION_NEEDED or DB_MIGRATION_RUNNING
        # status about migration are added to prevent the node to be kill while SonarQube is upgrading the database.
        if curl --noproxy "*" -s http://localhost:{{ .Values.service.internalPort }}{{ .Values.readinessProbe.sonarWebContext | default (include "sonarqube.webcontext" .) }}api/system/status | grep -q -e '"status":"UP"' -e '"status":"DB_MIGRATION_NEEDED"' -e '"status":"DB_MIGRATION_RUNNING"'; then
          exit 0
        fi
        exit 1
  initialDelaySeconds: 60
  periodSeconds: 30
  failureThreshold: 6
  # Note that timeoutSeconds was not respected before Kubernetes 1.20 for exec probes
  timeoutSeconds: 1
  # If an ingress *path* other than the root (/) is defined, it should be reflected here
  # A trailing "/" must be included
  # deprecated please use sonarWebContext at the value top level
  # sonarWebContext: /

livenessProbe:
  exec:
    command:
      - sh
      - -c
      - |
        curl --noproxy "*" -fsS -o /dev/null --max-time {{ .Values.livenessProbe.timeoutSeconds }} -H "X-Sonar-Passcode: $SONAR_WEB_SYSTEMPASSCODE" "http://localhost:{{ .Values.service.internalPort }}{{ .Values.livenessProbe.sonarWebContext | default (include "sonarqube.webcontext" .) }}api/system/liveness"
  initialDelaySeconds: 60
  periodSeconds: 30
  failureThreshold: 6
  # Note that timeoutSeconds was not respected before Kubernetes 1.20 for exec probes
  timeoutSeconds: 1
  # If an ingress *path* other than the root (/) is defined, it should be reflected here
  # A trailing "/" must be included
  # deprecated please use sonarWebContext at the value top level
  # sonarWebContext: /

startupProbe:
  initialDelaySeconds: 30
  periodSeconds: 10
  failureThreshold: 24
  # Note that timeoutSeconds was not respected before Kubernetes 1.20 for exec probes
  timeoutSeconds: 1
  # If an ingress *path* other than the root (/) is defined, it should be reflected here
  # A trailing "/" must be included
  # deprecated please use sonarWebContext at the value top level
  # sonarWebContext: /

initContainers:
  # all initContainers use SonarQube image by default, but you can override it by setting the image field (ex image: ubuntu:24.04)
  # image:
  # Set the security context for the init containers
  # The current section contains the default values set in a generic Kubernetes cluster. If you are using OpenShift, you should not set any specific UID or GID to be used for the execution.
  # We allow the init containers to have a separate security context declaration because
  # the initContainer may not require the same as SonarQube.
  # Those default are used to match pod security standard restricted as least privileged approach
  securityContext:
    allowPrivilegeEscalation: false
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 0
    seccompProfile:
      type: RuntimeDefault
    capabilities:
      drop: ["ALL"]
    readOnlyRootFilesystem: true
  # We allow the init containers to have a separate resources declaration because
  # the initContainer does not take as much resources.
  resources: {}

# Extra init containers to e.g. download required artifacts
extraInitContainers: {}

## Array of extra containers to run alongside the SonarQube container
##
## Example:
## - name: myapp-container
##   image: busybox
##   command: ['sh', '-c', 'echo Hello && sleep 3600']
##
extraContainers: []
extraVolumes: []
extraVolumeMounts: []

## Provide a secret containing one or more certificate files in the keys that will be added to cacerts
## The cacerts file will be set via SONARQUBE_WEB_JVM_OPTS and SONAR_CE_JAVAOPTS
##
caCerts:
  enabled: false
  # image:
  # secret: your-secret-name

  # Optionally, you can store your certificate in a ConfigMap and use it as:
  # configMap:
  #   name: my-custom-cacerts-certificate
  #   key: key
  #   path: my-certificate.crt

initSysctl:
  enabled: true
  vmMaxMapCount: 524288
  fsFileMax: 131072
  nofile: 131072
  nproc: 8192
  # all initContainers use SonarQube image by default, but you can override it by setting the image field (ex image: ubuntu:24.04)
  # image:
  securityContext:
    # Compatible with podSecurity standard privileged
    privileged: true
    # if run without root permissions, error "sysctl: permission denied on key xxx, ignoring"
    runAsUser: 0
    readOnlyRootFilesystem: true
  # resources: {}

# This should not be required anymore, used to chown/chmod folder created by faulty CSI driver that are not applying properly POSIX fsgroup.
initFs:
  enabled: true
  # all initContainers use SonarQube image by default, but you can override it by setting the image field (ex image: ubuntu:24.04)
  # image:
  # Compatible with podSecurity standard baseline.
  securityContext:
    privileged: false
    runAsNonRoot: false
    runAsUser: 0
    runAsGroup: 0
    seccompProfile:
      type: RuntimeDefault
    capabilities:
      drop: ["ALL"]
      add: ["CHOWN"]
    readOnlyRootFilesystem: true

prometheusExporter:
  enabled: false
  # jmx_prometheus_javaagent version to download from Maven Central
  version: "0.17.2"
  # Alternative full download URL for the jmx_prometheus_javaagent.jar (overrides prometheusExporter.version)
  # downloadURL: ""
  # if you need to ignore TLS certificates for whatever reason enable the following flag
  noCheckCertificate: false

  # Ports for the jmx prometheus agent to export metrics at
  webBeanPort: 8000
  ceBeanPort: 8001

  config:
    rules:
      - pattern: ".*"
  # Overrides config for the CE process Prometheus exporter (by default, the same rules are used for both the Web and CE processes).
  # ceConfig:
  #   rules:
  #     - pattern: ".*"
  # image: curlimages/curl:8.2.1
  # For use behind a corporate proxy when downloading prometheus
  # httpProxy: ""
  # httpsProxy: ""
  # noProxy: ""
  # Reuse default initcontainers.securityContext that match restricted pod security standard
  # securityContext: {}

prometheusMonitoring:
  # Generate a Prometheus Pod Monitor (https://github.com/coreos/prometheus-operator)
  #
  podMonitor:
    # Create PodMonitor Resource for Prometheus scraping
    enabled: false
    # (DEPRECATED) Specify a custom namespace where the PodMonitor will be created.
    # This value should not be set, as the PodMonitor's namespace has to match the Release Namespace.
    # namespace: "default"
    # Specify the interval how often metrics should be scraped
    interval: 30s
    # Specify the timeout after a scrape is ended
    # scrapeTimeout: ""
    # Name of the label on target services that prometheus uses as job name
    # jobLabel: ""
    # Additional labels to add to the PodMonitor
    # labels: {}

# List of plugins to install.
# For example:
# plugins:
#  install:
#    - "https://github.com/AmadeusITGroup/sonar-stash/releases/download/1.3.0/sonar-stash-plugin-1.3.0.jar"
#    - "https://github.com/SonarSource/sonar-ldap/releases/download/2.2-RC3/sonar-ldap-plugin-2.2.0.601.jar"
#
plugins:
  install: []

  # For use behind a corporate proxy when downloading plugins
  # httpProxy: ""
  # httpsProxy: ""
  # noProxy: ""

  # image: curlimages/curl:8.2.1
  # resources: {}

  # .netrc secret file with a key "netrc" to use basic auth while downloading plugins
  # netrcCreds: ""

  # Set to true to not validate the server's certificate to download plugin
  noCheckCertificate: false
  # Reuse default initcontainers.securityContext that match restricted pod security standard
  # securityContext: {}

## (DEPRECATED) The following value sets SONAR_WEB_JAVAOPTS (e.g., jvmOpts: "-Djava.net.preferIPv4Stack=true"). However, this is deprecated, please set SONAR_WEB_JAVAOPTS or sonar.web.javaOpts directly instead.
jvmOpts: ""

## (DEPRECATED) The following value sets SONAR_CE_JAVAOPTS. However, this is deprecated, please set SONAR_CE_JAVAOPTS or sonar.ce.javaOpts directly instead.
jvmCeOpts: ""

## a monitoring passcode needs to be defined in order to get reasonable probe results
# not setting the monitoring passcode will result in a deployment that will never be ready
# monitoringPasscode: "define_it"
# Alternatively, you can define the passcode loading it from an existing secret specifying the right key
# monitoringPasscodeSecretName: "pass-secret-name"
# monitoringPasscodeSecretKey: "pass-key"

## Environment variables to attach to the pods
##
# env:
#   # If you use a different ingress path from /, you have to add it here as the value of SONAR_WEB_CONTEXT
#   - name: SONAR_WEB_CONTEXT
#     value: /sonarqube
#   - name: VARIABLE
#     value: my-value

# Set annotations for pods
annotations: {}

## We usually don't make specific resource recommendations, as they are heavily dependant on
## the usage of SonarQube and the surrounding infrastructure.
## Those default are based on the default Web -Xmx1G -Xms128m and CE -Xmx2G -Xms128m and Search -Xmx2G -Xms2G settings of SQ sub processes
## Adjust these values to your needs, you can find more details on the main README of the chart.
resources:
  limits:
    cpu: 800m
    memory: 6144M
    ephemeral-storage: 512000M
  requests:
    cpu: 400m
    memory: 2048M
    ephemeral-storage: 1536M

persistence:
  enabled: false
  ## Set annotations on pvc
  annotations: {}

  ## Specify an existing volume claim instead of creating a new one.
  ## When using this option all following options like storageClass, accessMode and size are ignored.
  # existingClaim:

  ## If defined, storageClassName: <storageClass>
  ## If set to "-", storageClassName: "", which disables dynamic provisioning
  ## If undefined (the default) or set to null, no storageClassName spec is
  ##   set, choosing the default provisioner.  (gp2 on AWS, standard on
  ##   GKE, AWS & OpenStack)
  ##
  storageClass:
  accessMode: ReadWriteOnce
  size: 5Gi
  uid: 1000
  guid: 0

  ## DEPRECATED please use root level extraVolumes value
  ## Specify extra volumes. Refer to ".spec.volumes" specification : https://kubernetes.io/fr/docs/concepts/storage/volumes/
  volumes: []
  ## DEPRECATED please use root level extraVolumeMounts value
  ## Specify extra mounts. Refer to ".spec.containers.volumeMounts" specification : https://kubernetes.io/fr/docs/concepts/storage/volumes/
  mounts: []

  # In case you want to specify hostPath for to storage into the host.
  # hostPath:
  #   # Path where the data will be stored.
  #   path: /data/sonarqube/
  #   # Type of volume. Must be one of: DirectoryOrCreate, Directory, FileOrCreate, File, Socket, CharDevice, BlockDevice.
  #   type: DirectoryOrCreate

# In case you want to specify different resources for emptyDir than {}
emptyDir:
  {}
  # Example of resouces that might be used:
  # medium: Memory
  # sizeLimit: 16Mi

# A custom sonar.properties file can be provided via dictionary.
# For example:
# sonarProperties:
#   sonar.log.level: DEBUG
#   sonar.security.realm: LDAP
#   ldap.url: ldaps://organization.com

# Additional sonar properties to load from a secret with a key "secret.properties" (must be a string)
# sonarSecretProperties:

# Kubernetes secret that contains the encryption key for the SonarQube instance.
# The secret must contain the key 'sonar-secret.txt'.
# The 'sonar.secretKeyPath' property will be set automatically.
# When agentOrchestrator.enabled, this secret is also mounted into the orchestrator, with its
# path exposed via the AGENTIC_SECRET_KEY_PATH env var.
# sonarSecretKey: "settings-encryption-secret"

## Override JDBC values
## for external Databases
jdbcOverwrite:
  # (DEPRECATED) Please use jdbcOverwrite.enabled instead
  # enable: false
  enabled: false
  # The JDBC url of the external DB
  jdbcUrl: "jdbc:postgresql://myPostgres/myDatabase"
  # The DB user that should be used for the JDBC connection
  jdbcUsername: "sonarUser"
  # Use this if you don't mind the DB password getting stored in plain text within the values file
  # (DEPRECATED) Please use `jdbcOverwrite.jdbcSecretName` along with `jdbcOverwrite.jdbcSecretPasswordKey` instead
  jdbcPassword: "sonarPass"
  ## Alternatively, use a pre-existing k8s secret containing the DB password
  # jdbcSecretName: "sonarqube-jdbc"
  ## and the secretValueKey of the password found within that secret
  # jdbcSecretPasswordKey: "jdbc-password"
  # To install the oracle JDBC driver, set the following URL (in this example, we set the URL for the Oracle 11 driver. Please update it to your target driver URL.).
  # If downloading the driver requires authentication, please set the .netrc secret file with a key "netrc" to use basic auth.
  # oracleJdbcDriver:
  #   url: "https://download.oracle.com/otn-pub/otn_software/jdbc/2113/ojdbc11.jar"
  #   netrcCreds: ""

# Additional labels to add to the pods:
# podLabels:
#   key: value
podLabels: {}
# (DEPRECATED) this field will be removed, as it needs to match the SonarQube image folder structure, considering we have one chart version per docker image version this field is not needed anymore.
sonarqubeFolder: /opt/sonarqube

tests:
  image: ""
  enabled: true
  resources:
    requests:
      cpu: 500m
      memory: 200M
      ephemeral-storage: 100M
    limits:
      cpu: 500m
      memory: 200M
      ephemeral-storage: 1000M

# For OpenShift set create=true to ensure service account is created.
serviceAccount:
  create: false
  # name:
  automountToken: false
  ## Annotations for the Service Account
  annotations: {}

# extraConfig is used to load Environment Variables from Secrets and ConfigMaps
# which may have been written by other tools, such as external orchestrators.
#
# These Secrets/ConfigMaps are expected to contain Key/Value pairs, such as:
#
# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: external-sonarqube-opts
# data:
#   SONARQUBE_JDBC_USERNAME: foo
#   SONARQUBE_JDBC_URL: jdbc:postgresql://db.example.com:5432/sonar
#
# These vars can then be injected into the environment by uncommenting the following:
#
# extraConfig:
#   configmaps:
#     - external-sonarqube-opts

extraConfig:
  secrets: []
  configmaps: []

# setAdminPassword:
# The values can be set to define the current and the (new) custom admin passwords at the startup (the username will remain "admin")
#  newPassword: AdminAdmin_12$
#  currentPassword: admin
# The above values can be also provided by a secret that contains "password" and "currentPassword" as keys. You can generate such a secret in your cluster
# using "kubectl create secret generic admin-password-secret-name --from-literal=password=admin --from-literal=currentPassword=admin"
#  passwordSecretName: ""
# Reuse default initcontainers.securityContext that match restricted pod security standard
#  securityContext: {}
#  resources:
#    limits:
#      cpu: 100m
#      memory: 128Mi
#    requests:
#      cpu: 100m
#      memory: 128Mi
#  image:
#  annotations: {}

# (DEPRECATED) please use setAdminPassword instead
# account:
# The values can be set to define the current and the (new) custom admin passwords at the startup (the username will remain "admin")
#   adminPassword: AdminAdmin_12$
#   currentAdminPassword: admin
# The above values can be also provided by a secret that contains "password" and "currentPassword" as keys. You can generate such a secret in your cluster
# using "kubectl create secret generic admin-password-secret-name --from-literal=password=admin --from-literal=currentPassword=admin"
#   adminPasswordSecretName: ""
# Reuse default initcontainers.securityContext that match restricted pod security standard
#   securityContext: {}
#   resources:
#     limits:
#       cpu: 100m
#       memory: 128Mi
#     requests:
#       cpu: 100m
#       memory: 128Mi
# (DEPRECATED) please use setAdminPassword.image instead
# curlContainerImage: curlimages/curl:8.2.1
# (DEPRECATED) please use setAdminPassword.annotations instead
# adminJobAnnotations: {}
# deprecated please use sonarWebContext at the value top level
#   sonarWebContext: /

# (DEPRECATED) This value is not used in the templates.
terminationGracePeriodSeconds: 60

# MCP (Model Context Protocol) server sidecar deployment.
# When enabled, a separate MCP server is deployed alongside SonarQube and
# SonarQube is automatically configured to connect to it via SONAR_MCP_ENABLED
# and SONAR_MCP_SERVERURL environment variables.
mcp:
  enabled: false

  image:
    repository: sonarsource/sonarqube-mcp
    tag: "1.22.0.3040"
    pullPolicy: IfNotPresent
    # pullSecrets:
    #   - name: my-repo-secret

  port: 8080

  # healthCheckInterval sets SONAR_MCP_HEALTHCHECKINTERVAL on the SonarQube side (in seconds).
  # healthCheckInterval: "30"

  persistence:
    enabled: true
    ## Set annotations on pvc
    annotations: {}
    ## Specify an existing volume claim instead of creating a new one.
    ## When using this option storageClass, accessMode and size are ignored.
    # existingClaim: ""
    ## If defined, storageClassName: <storageClass>
    ## If set to "-", storageClassName: "", which disables dynamic provisioning
    storageClass:
    accessMode: ReadWriteOnce
    size: 1Gi

  # Set security context for the MCP container.
  containerSecurityContext:
    allowPrivilegeEscalation: false
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 0
    seccompProfile:
      type: RuntimeDefault
    capabilities:
      drop: ["ALL"]

  livenessProbe:
    initialDelaySeconds: 60
    periodSeconds: 30
    failureThreshold: 6
    timeoutSeconds: 1

  # TLS configuration for encrypted communication between SonarQube and MCP.
  # When enabled, the MCP server uses HTTPS transport and SonarQube connects via https://.
  # Requires a PKCS12 or JKS keystore stored in a Kubernetes Secret.
  tls:
    enabled: false
    # Secret containing the keystore file.
    keystoreSecretName: ""
    keystoreSecretKey: "keystore.p12"
    # Secret containing the keystore password.
    passwordSecretName: ""
    passwordSecretKey: "password"
    # Keystore type: PKCS12 or JKS.
    keystoreType: "PKCS12"

  # Additional environment variables for the MCP container.
  env: []
  # - name: MY_VAR
  #   value: my-value

  resources: {}
  # limits:
  #   cpu: 200m
  #   memory: 256Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

  # Set annotations for the MCP pod.
  annotations: {}

# Vortex analysis: an analysis service that SonarQube calls on demand to analyse individual
# files. When enabled, the chart points the SonarQube application nodes at it via the
# sonar.vortex.analysis.url property.
vortex:
  enabled: false

  image:
    repository: ""
    tag: ""
    pullPolicy: IfNotPresent
    # pullSecret: my-repo-secret
    # pullSecrets:
    #   - name: my-repo-secret

  port: 8080

  replicaCount: 1

  # Update strategy for the Vortex Deployment. Recreate avoids running two pods at once
  # during an upgrade.
  strategy:
    type: Recreate

  # Token Vortex analysis uses to call the SonarQube Web API. Required for analysis to succeed.
  sonarqubeToken:
    # The token itself. Ignored when existingSecret is set.
    token: ""
    # Name of an existing Secret containing the token.
    existingSecret: ""
    # Key inside existingSecret. Defaults to VORTEX_ANALYSIS_SONARQUBE_TOKEN.
    existingSecretKey: ""

  # S3-compatible object storage for Vortex analysis context restoration. Must point at the same
  # bucket SonarQube Server writes context to (sonar.agentic.storage.*).
  storage:
    type: S3
    bucket: ""
    region: ""
    # false for AWS S3, true for most S3-compatible endpoints (e.g. MinIO).
    pathStyleAccess: false
    # Custom S3-compatible endpoint (e.g. MinIO). Leave blank for real AWS S3.
    endpoint: ""
    # Credentials for that endpoint. Leave accessKey/secretKey/existingSecret all blank for
    # credential-less access: the AWS SDK then falls back to its default credential chain (the
    # pod's own IAM identity - node instance role, or IRSA via vortex.serviceAccount).
    accessKey: ""
    secretKey: ""
    # Existing secret providing SONAR_AGENTIC_STORAGE_ACCESS_KEY / SONAR_AGENTIC_STORAGE_SECRET_KEY
    existingSecret: ""

  # ServiceAccount for the Vortex pod, independent of the top-level serviceAccount.
  serviceAccount:
    create: false
    name: ""
    automountToken: false
    annotations: {}

  # Set security context for the Vortex container.
  containerSecurityContext:
    allowPrivilegeEscalation: false
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 0
    seccompProfile:
      type: RuntimeDefault
    capabilities:
      drop: ["ALL"]

  # Additional environment variables for the Vortex container.
  env: []
  # - name: MY_VAR
  #   value: my-value

  resources: {}

  extraVolumes: []
  extraVolumeMounts: []

  # Wins over the top-level .Values.nodeSelector when set.
  nodeSelector: {}
  # Wins over the top-level .Values.affinity when set.
  affinity: {}
  # Wins over the top-level .Values.tolerations when set.
  tolerations: []

  # Set annotations for the Vortex pod.
  annotations: {}

# Shared Agent Orchestrator, dispatching jobs to hunterAgent/remediationAgent. hunterAgent and
# remediationAgent each require this to be enabled - see validation.yaml. The orchestrator shares
# SonarQube's database, so it also requires jdbcOverwrite.enabled=true: the embedded database that
# jdbcOverwrite.enabled=false (the default here) selects is not reachable from another pod.
agentOrchestrator:
  enabled: false

  image:
    repository: ""
    tag: ""
    pullPolicy: IfNotPresent
    # For the orchestrator's own image only; its wait-for-sonarqube init container pulls the
    # SonarQube image instead, and always gets image.pullSecret(s) for that.
    # pullSecrets:
    #   - name: my-repo-secret
  port: 8080
  replicaCount: 1
  # Scheduling knobs for the orchestrator pod. Each wins over the chart's global one of the same
  # name (.Values.nodeSelector/tolerations/affinity) when set.
  nodeSelector: {}
  tolerations: []
  affinity: {}
  # How many old ReplicaSets to retain. Blank uses the Kubernetes default (10).
  revisionHistoryLimit: ""
  # Deployment update strategy. Blank uses the Kubernetes default (RollingUpdate).
  strategy: {}
  # Extra labels/annotations for the orchestrator pod.
  podLabels: {}
  annotations: {}
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
      ephemeral-storage: 512Mi
    limits:
      cpu: "1"
      memory: 1Gi
      ephemeral-storage: 2Gi
  securityContext: {}

  probes:
    readiness:
      enabled: true
      path: /health
      initialDelaySeconds: 5
      periodSeconds: 10
    liveness:
      enabled: true
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 30

  containerSecurityContext:
    allowPrivilegeEscalation: false
    runAsNonRoot: true
    runAsUser: 900
    runAsGroup: 900
    seccompProfile:
      type: RuntimeDefault
    capabilities:
      drop: ["ALL"]
    readOnlyRootFilesystem: true

  # Enable the orchestrator's internal scheduler. When true, injects
  # SONAR_AGENTIC_ORCHESTRATOR_SCHEDULER_ENABLED=true on the orchestrator container.
  scheduler:
    enabled: false

  # ServiceAccount for the orchestrator pod, independent of the top-level serviceAccount. `name`
  # pins the SA name so an external IAM trust policy can reference an exact k8s subject; defaults
  # to <fullname>-agent-orchestrator.
  serviceAccount:
    create: false
    name: ""
    automountToken: false
    annotations: {}

  # CORE DB (SonarQube's database) connection for the orchestrator (CORE_DB_* env). Fully decoupled
  # from SonarQube's own jdbcOverwrite: each field below defaults to the jdbcOverwrite-derived value
  # (so out of the box the orchestrator targets the same DB as SonarQube), but can be set explicitly.
  # In particular the password must be a value the orchestrator can use directly - SonarQube may
  # store an encrypted `{aes-gcm}` password it can't decrypt, or a chart-managed secret your GitOps
  # doesn't apply - so point passwordSecretName/passwordSecretKey at a usable secret.
  coreDb:
    endpoint: ""            # host:port; default parsed from jdbcOverwrite.jdbcUrl
    name: ""                # database name; default parsed from jdbcOverwrite.jdbcUrl
    username: ""            # default jdbcOverwrite.jdbcUsername
    passwordSecretName: ""  # default: the jdbcOverwrite-derived secret name
    passwordSecretKey: ""   # default: the jdbcOverwrite-derived password key

  # Object storage (S3/MinIO) for the shared job folder -> AGENTIC_STORAGE_* env
  storage:
    # Custom S3 endpoint (e.g. MinIO). Leave blank for real AWS S3 so the SDK uses its regional
    # endpoint. pathStyle should be true for MinIO, false for AWS S3 (virtual-hosted style).
    endpoint: ""
    region: us-east-1
    bucket: agent-jobs
    pathStyle: true
    # Credentials. Leave accessKey/secretKey/existingSecret all blank for credential-less access:
    # the orchestrator's AWS SDK then falls back to its default credential chain (the pod's IAM
    # identity - node instance role, or IRSA via agentOrchestrator.serviceAccount annotations).
    # Inline access/secret key; ignored when existingSecret is set
    accessKey: ""
    secretKey: ""
    # Existing secret providing AGENTIC_STORAGE_ACCESS_KEY / AGENTIC_STORAGE_SECRET_KEY
    existingSecret: ""

  # Bearer token for AGENTIC_SONARQUBE_URL's /api/issues/search, used by the remediation
  # dispatcher to enrich issues before pushing a job. Blank -> anonymous access (fails against a
  # SonarQube instance that requires auth; the remediation job then fails at dispatch).
  sonarqubeToken:
    # Inline token; ignored when existingSecret is set -> AGENTIC_SONARQUBE_TOKEN
    token: ""
    # Existing secret providing the token
    existingSecret: ""
    # Key within existingSecret; defaults to AGENTIC_SONARQUBE_TOKEN
    existingSecretKey: ""

  # Remediation flow: push a fix branch and open a PR via the GitHub REST API. No token is
  # configured here: since SONAR-30903 the orchestrator obtains a short-lived GitHub App
  # installation token from SonarQube on demand (via sonarqubeToken above), right
  # before each git operation. Configure a GitHub App DevOps integration on SonarQube and bind
  # the project for the PR flow to succeed.
  github:
    # AGENTIC_GITHUB_API_BASE_URL; blank -> orchestrator's own default (real api.github.com).
    # Override for GitHub Enterprise Server, or to point at a local mock.
    apiBaseUrl: ""

# Hunter Agent runtime that detects issues. Requires agentOrchestrator.enabled - see validation.yaml.
# Process-count limits aren't set here - use your node's kubelet configuration (podPidsLimit).
hunterAgent:
  enabled: false
  image:
    repository: ""
    tag: ""
    pullPolicy: IfNotPresent
  port: 8090
  # >1 enables the L4 429 re-routing: the Service is a load-balanced ClusterIP (not
  # headless) so a rejected request's retry gets a fresh TCP handshake routed to another
  # replica by kube-proxy.
  replicaCount: 1
  # Scheduling knobs for this runtime's pod. Each wins over the chart's global one of the same
  # name (.Values.nodeSelector/tolerations/affinity) when set. gvisor.nodeSelector is merged in
  # separately by the RuntimeClass when gVisor is enabled - see gvisor.nodeSelector in values.yaml.
  nodeSelector: {}
  tolerations: []
  affinity: {}
  # How many old ReplicaSets to retain. Blank uses the Kubernetes default (10).
  revisionHistoryLimit: ""
  # Deployment update strategy. Blank uses the Kubernetes default (RollingUpdate).
  strategy: {}
  # Extra labels/annotations for this runtime's pod.
  podLabels: {}
  annotations: {}
  resources: {}
  securityContext: {}
  probes:
    readiness:
      enabled: true
      path: /readyz
      initialDelaySeconds: 5
      periodSeconds: 10
    liveness:
      enabled: true
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 30
  # No readOnlyRootFilesystem: the image needs its home directory writable.
  containerSecurityContext:
    allowPrivilegeEscalation: false
    runAsNonRoot: true
    runAsUser: 10001
    runAsGroup: 10001
    seccompProfile:
      type: RuntimeDefault
    capabilities:
      drop: ["ALL"]
  # ServiceAccount for this runtime, independent of the top-level serviceAccount. `name` pins the
  # SA name for an external IAM trust policy; defaults to <fullname>-agent-runtime-hunter.
  serviceAccount:
    create: false
    name: ""
    automountToken: false
    annotations: {}
  # Restrict traffic to and from this runtime.
  networkPolicy:
    enabled: false
  # Path to the detection-agent entrypoint inside the image; the chart exposes it as
  # SCRIPT_PATH, which switches the runtime into hunter/detection mode (symmetric with
  # remediation's remediationScriptPath -> REMEDIATION_SCRIPT_PATH). Override if the image
  # relocates it; set to "" to omit the env var entirely. Unlike remediation, the hunter
  # runtime never calls the orchestrator for rule-info, so no rule-info endpoint is wired.
  scriptPath: /home/agent/app/.venv/bin/detection-agent
  # Extra env vars for the runtime container. The chart only sets `port` on the Service /
  # containerPort - it does not assume a listen-port convention for arbitrary runtime images
  # (Spring Boot images read SERVER_PORT; others may hardcode their port or use something
  # else), so set whatever env var your image needs here to make it actually listen on
  # `port`. Also where you'd set an image-specific LLM endpoint override, etc.
  env: []
  # - name: SERVER_PORT
  #   value: "8090"
  # Extra volumes/mounts for the runtime container/pod - e.g. a custom CA cert for a network
  # that TLS-inspects egress to the LLM endpoint (the JVM/language runtime's own trust store is
  # separate from the OS's, so a system-level fix alone isn't enough; mount the cert here and
  # have the image import it at startup, same idea as this chart's own caCerts feature).
  extraVolumes: []
  # - name: custom-ca
  #   secret:
  #     secretName: my-runtime-custom-ca
  extraVolumeMounts: []
  # - name: custom-ca
  #   mountPath: /custom-ca
  #   readOnly: true

# Remediation Agent runtime that remediates issues. Requires agentOrchestrator.enabled AND
# vortex.enabled - see validation.yaml.
remediationAgent:
  enabled: false  # enable once the remediation runtime image is available
  image:
    repository: ""
    tag: ""
    pullPolicy: IfNotPresent
  port: 8090
  replicaCount: 1
  # Scheduling knobs for this runtime's pod. Each wins over the chart's global one of the same
  # name (.Values.nodeSelector/tolerations/affinity) when set. gvisor.nodeSelector is merged in
  # separately by the RuntimeClass when gVisor is enabled - see gvisor.nodeSelector in values.yaml.
  nodeSelector: {}
  tolerations: []
  affinity: {}
  # How many old ReplicaSets to retain. Blank uses the Kubernetes default (10).
  revisionHistoryLimit: ""
  # Deployment update strategy. Blank uses the Kubernetes default (RollingUpdate).
  strategy: {}
  # Extra labels/annotations for this runtime's pod.
  podLabels: {}
  annotations: {}
  # Defaults sized for the SonarQube Remediation Agent (SQRA), which clones repos and runs the
  # agent/LLM loop. Override for your workload.
  resources:
    requests:
      cpu: "1"
      memory: 2Gi
      ephemeral-storage: 10Gi
    limits:
      cpu: "4"
      memory: 8Gi
      ephemeral-storage: 50Gi
  securityContext: {}
  probes:
    readiness:
      enabled: true
      path: /readyz
      initialDelaySeconds: 5
      periodSeconds: 10
    liveness:
      enabled: true
      path: /health
      initialDelaySeconds: 10
      periodSeconds: 30
  # No readOnlyRootFilesystem: the image needs its home directory writable.
  containerSecurityContext:
    allowPrivilegeEscalation: false
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    seccompProfile:
      type: RuntimeDefault
    capabilities:
      drop: ["ALL"]
  # ServiceAccount for this runtime, independent of the top-level serviceAccount. `name` pins the
  # SA name for an external IAM trust policy; defaults to <fullname>-agent-runtime-remediation.
  serviceAccount:
    create: false
    name: ""
    automountToken: false
    annotations: {}
  # Restrict traffic to and from this runtime.
  networkPolicy:
    enabled: false
  # Path to the remediation-agent entrypoint inside the image; the chart exposes it as
  # REMEDIATION_SCRIPT_PATH, which switches the runtime into remediation mode. Override if the
  # image relocates it (e.g. a Python version bump); set to "" to omit the env var entirely.
  # (REMEDIATION_RULE_INFO_ENDPOINT is also auto-wired, derived from the Agent Orchestrator URL.)
  remediationScriptPath: /home/agent/app/.venv/lib/python3.13/site-packages/remediation_agent/main.py
  env: []
  extraVolumes: []
  extraVolumeMounts: []

# Squid forward-proxy that all agent runtime (hunterAgent/remediationAgent) egress is routed
# through, so FQDN allow-listing (Squid dstdomain ACLs) becomes possible where NetworkPolicy alone
# cannot express it (L3/L4 only, no hostname matching). Always a standalone Deployment, never a
# sidecar (NetworkPolicy is enforced per pod - see templates/agent-egress-proxy.yaml).
#
# This component has no independent enable/disable switch: it is automatically deployed whenever
# hunterAgent.enabled or remediationAgent.enabled is true, and renders nothing when both are
# false. There is no way to enable a runtime without the proxy, or vice versa.
agentEgressProxy:
  image:
    repository: ubuntu/squid
    # Canonical's Ubuntu LTS Squid rock. `digest` wins over `tag` when both are set, and is what
    # actually gets deployed here - the tag is kept only as human-readable provenance.
    #
    # Pinning matters more than usual for this image: Canonical's channel naming is
    # <squidVersion>-<ubuntuRelease>_<risk>, and `_edge` is a *floating* risk channel that gets
    # rebuilt in place. The 6.6-24.04_edge tag already resolves to Squid 6.14, not 6.6, so the tag
    # alone is not a reproducible reference. Bump both together, deliberately, after re-checking
    # `docker buildx imagetools inspect ubuntu/squid:<tag>`.
    tag: "6.6-24.04_edge"
    # Multi-arch index digest for 6.6-24.04_edge (amd64/arm64/ppc64le/s390x), resolved 2026-08-27.
    digest: sha256:8a3baed477e2c282ab8aa5edad442f69873246964f225c5c2ae8364b6610963c
    pullPolicy: IfNotPresent
    pullSecret: ""
    pullSecrets: []
  port: 3128
  # D7: default 2 so a single pod eviction/restart/drain doesn't cut off every runtime's egress.
  # Override to 1 for local/dev/kind if the single point of failure is acceptable there - also set
  # podDisruptionBudget: null then, or minAvailable: 1 will block every node drain.
  replicaCount: 2
  nodeSelector: {}
  tolerations: []
  # Left empty so the chart can apply a soft (preferred) podAntiAffinity on kubernetes.io/hostname:
  # with replicaCount 2 and podDisruptionBudget.minAvailable 1, two co-scheduled replicas would let
  # a single node drain cut all runtime egress while the PDB simultaneously blocks that drain.
  # Setting a value here replaces that default outright - it is not merged into.
  affinity: {}
  revisionHistoryLimit: ""
  strategy: {}
  podLabels: {}
  annotations: {}
  # D7 makes this a hard dependency of every runtime's data path (unlike hunterAgent's own
  # `resources: {}` default) - Squid caching is disabled (no cache_dir), so memory/storage
  # needs stay small regardless of traffic volume.
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
      ephemeral-storage: 64Mi
    limits:
      cpu: 250m
      memory: 128Mi
      ephemeral-storage: 128Mi
  securityContext: {}
  containerSecurityContext:
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    # The pinned ubuntu/squid image declares no non-root USER (it starts as root by convention,
    # matching its own cache_effective_user startup-then-drop-privileges model), so runAsNonRoot
    # requires an explicit runAsUser or the container fails to start (CreateContainerConfigError).
    # 13 is the image's built-in "proxy" uid/gid — verified to start squid cleanly with no cache_dir
    # and no TLS interception (D6), matching the cache_effective_user directive in squid.conf below.
    runAsUser: 13
    runAsGroup: 13
    seccompProfile:
      type: RuntimeDefault
    capabilities:
      drop: ["ALL"]
  probes:
    readiness:
      periodSeconds: 10
      timeoutSeconds: 5
    liveness:
      periodSeconds: 10
      timeoutSeconds: 5
  serviceAccount:
    create: false
    name: ""
    automountToken: false
    annotations: {}
  # Domains runtimes may reach through the proxy (Squid `dstdomain` ACL). A leading dot means
  # "this domain and all subdomains". HTTPS is CONNECT-tunneled (no TLS interception in v1) -
  # matched against the CONNECT target hostname, not decrypted SNI.
  # Leaving this empty is a valid but near-useless configuration: the runtimes can still reach
  # SonarQube's own agentic endpoints (hardcoded, see squid.conf) but nothing else, so no external
  # LLM provider is callable. Set at least the provider domain the runtimes are pointed at.
  allowedDomains: []
  # - .anthropic.com
  # Raw squid.conf lines spliced in after the generated allow rules but *before* the final
  # `http_access deny all`, so an `http_access allow ...` added here does take effect. Squid stops
  # at the first matching http_access line, so ordering within this block still matters.
  #
  # This is also how you reach an endpoint on a port other than 80/443 - Squid resolves
  # http_access rules to the named ACL object, so appending here still applies to the earlier
  # `deny !Safe_ports` rule. Mirror any such port in networkPolicy.egressPorts below:
  #   extraSquidConf: |
  #     acl Safe_ports port 8443
  #     acl SSL_ports port 8443
  #
  # Unvalidated: a syntax error here means Squid refuses to start, which takes out egress for
  # every runtime. `squid -k parse -f squid.conf` against the pinned image before rolling out.
  extraSquidConf: ""
  networkPolicy:
    enabled: false
    egressPorts: [80, 443]
    # Destinations subtracted from the broad 0.0.0.0/0 egress rule. NetworkPolicy cannot match
    # FQDNs, so that rule has to stay wide and Squid's allowedDomains does the real filtering;
    # these CIDRs are the belt-and-braces case where that filtering must not be the only control.
    # 169.254.0.0/16 is link-local, which carries the cloud metadata service (169.254.169.254) on
    # AWS/GCP/Azure - a classic SSRF pivot. Add RFC1918 ranges here too if no allowed destination
    # is on the private network (do NOT if the LLM is reached via an on-prem gateway).
    egressExcludeCidrs:
      - 169.254.0.0/16
  podDisruptionBudget:
    minAvailable: 1
  env: []
  extraVolumes: []
  extraVolumeMounts: []

# gVisor (runsc) sandboxing for the agent runtimes. Renders only when gvisor.enabled is true
# AND at least one of hunterAgent.enabled / remediationAgent.enabled is true. See README ("gVisor
# sandboxing").
gvisor:
  enabled: true
  # Name of the RuntimeClass created + referenced by runtimes. RuntimeClass is cluster-scoped:
  # any two installs sharing this value will collide (the second install fails, RuntimeClass owned
  # by the first release) — give each a distinct runtimeClassName. That covers multiple
  # releases/namespaces of this chart, and this chart installed alongside sonarqube-dce, which
  # ships the same feature.
  runtimeClassName: gvisor
  handler: runsc                    # containerd runtime handler
  # RuntimeClass.scheduling.nodeSelector, and (when installer.enabled) the label the installer
  # applies once runsc is ready. Use `null`, not `{}`, to disable node pinning — Helm merges maps.
  nodeSelector:
    gvisor.enabled: "true"
  installer:
    enabled: true                   # deploy the privileged installer DaemonSet (needs gvisor.enabled)
    image:
      repository: debian
      tag: stable-slim
      # Pinned for a reproducible pull; bump deliberately. Set to "" to use repository:tag.
      digest: sha256:1710bde34461551a19a47c787885ec9ad7058d9a5bead2affb8d088fa2f8502b
      pullPolicy: IfNotPresent
      # pullSecrets:
      #   - name: my-repo-secret
    # Pinned gVisor release for reproducible installs (not "latest"); bump deliberately.
    # See https://github.com/google/gvisor/tags for the release list.
    runscVersion: "20260706"
    containerdConfigPath: /etc/containerd/config.toml
    # Rendered to /etc/containerd/runsc.toml. network=host avoids gVisor's netstack breaking
    # cluster DNS/kube-proxy — unrelated to Kubernetes hostNetwork; pod isolation is unaffected.
    runscConfig:
      network: host
      overlay2: "root:self,size=50g"
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
        ephemeral-storage: 50Mi
      limits:
        cpu: 500m
        memory: 256Mi
        ephemeral-storage: 200Mi
    nodeSelector: {}                # which nodes to install on (empty = all)
    tolerations:                    # run everywhere incl. tainted/control-plane nodes
      - operator: Exists
    annotations: {}
