{{- $token := .Values.vortex.sonarqubeToken }}
{{- $storage := .Values.vortex.storage }}
{{- $inlineToken := and (not $token.existingSecret) $token.token }}
{{- $inlineStorage := and (not $storage.existingSecret) (or $storage.accessKey $storage.secretKey) }}
{{- if and .Values.vortex.enabled (or $inlineToken $inlineStorage) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sonarqube.vortex.fullname" . }}
  labels: {{- include "sonarqube.labels" . | nindent 4 }}
    app.kubernetes.io/component: vortex
type: Opaque
data:
  {{- if $inlineToken }}
  VORTEX_ANALYSIS_SONARQUBE_TOKEN: {{ $token.token | b64enc | quote }}
  {{- end }}
  {{- if $inlineStorage }}
  SONAR_AGENTIC_STORAGE_ACCESS_KEY: {{ $storage.accessKey | b64enc | quote }}
  SONAR_AGENTIC_STORAGE_SECRET_KEY: {{ $storage.secretKey | b64enc | quote }}
  {{- end }}
{{- end }}
