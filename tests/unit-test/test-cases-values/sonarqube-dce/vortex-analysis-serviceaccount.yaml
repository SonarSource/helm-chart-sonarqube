monitoringPasscode: test-passcode
applicationNodes:
  jwtSecret: test-jwt-secret
jdbcOverwrite:
  jdbcUrl: jdbc:postgresql://test-host:5432/testdb
  jdbcUsername: test-user
  jdbcPassword: test-password
# Vortex analysis with a dedicated, IRSA-annotated ServiceAccount.
vortexAnalysis:
  enabled: true
  image:
    repository: example.com/a3s/analysis/proxysqs
    tag: "7593"
  sonarqubeToken:
    token: "squ_example000000000000000000000000000000"
  storage:
    bucket: agentic-artifacts
    region: eu-west-1
  serviceAccount:
    create: true
    automountToken: true
    annotations:
      eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/vortex-analysis
