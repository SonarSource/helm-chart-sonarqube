community:
  enabled: true
monitoringPasscode: test-passcode
# Vortex with the token taken from an existing Secret.
vortex:
  enabled: true
  image:
    repository: example.com/vortex
    tag: "7593"
  sonarqubeToken:
    existingSecret: my-vortex-token
    existingSecretKey: TOKEN
  storage:
    bucket: agent-artifacts
    region: eu-west-1
