community:
  enabled: true
monitoringPasscode: test-passcode
# Vortex enabled without an image repository, which must fail validation.
vortex:
  enabled: true
  image:
    repository: ""
    tag: "7593"
