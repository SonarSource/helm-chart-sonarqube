community:
  enabled: true
monitoringPasscode: test-passcode
jdbcOverwrite:
  enabled: true
  jdbcUrl: jdbc:postgresql://test-host:5432/testdb
  jdbcUsername: test-user
  jdbcPassword: test-password
agentOrchestrator:
  enabled: true
  image:
    repository: example.com/agent-orchestrator
    tag: "42"
  storage:
    bucket: agent-jobs
hunterAgent:
  enabled: true
  image:
    repository: example.com/hunter-agent
    tag: "1"
  networkPolicy:
    enabled: true
remediationAgent:
  enabled: true
  image:
    repository: example.com/remediation-agent
    tag: "1"
  networkPolicy:
    enabled: true
vortexAnalysis:
  enabled: true
  image:
    repository: example.com/vortex
    tag: "1"
  storage:
    bucket: vortex-artifacts
    region: eu-west-1
agenticSigningSecret:
  existingSecret: test-agentic-instance-secret
gvisor:
  enabled: true
istio:
  enabled: true
  meshSidecar:
    enabled: false
  # A real address so overriding gvisor.enabled=false here (standard injection, so an Envoy
  # after all) doesn't hit the fail-closed "auto with no live cluster" check (validation.yaml).
  istiodClusterIP: "10.96.0.42"
