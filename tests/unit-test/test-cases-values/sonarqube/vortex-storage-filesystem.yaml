community:
  enabled: true
monitoringPasscode: test-passcode
# Vortex on a shared filesystem instead of an object store - no bucket/region needed.
vortex:
  enabled: true
  image:
    repository: example.com/vortex
    tag: "7593"
  sonarqubeToken:
    token: "squ_example000000000000000000000000000000"
  storage:
    type: FILESYSTEM
    filesystem:
      baseDir: /agentic-storage
