---
# Source: sonarqube/templates/secret.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: duplicated-env-values.yaml-sonarqube-monitoring-passcode
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
type: Opaque
data:
  SONAR_WEB_SYSTEMPASSCODE: "dGVzdA=="
---
# Source: sonarqube/templates/secret.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: duplicated-env-values.yaml-sonarqube-http-proxies
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
type: Opaque
stringData:
  PLUGINS-HTTP-PROXY: ""
  PLUGINS-HTTPS-PROXY: ""
  PLUGINS-NO-PROXY: ""
  PROMETHEUS-EXPORTER-HTTP-PROXY: "http://someProxy"
  PROMETHEUS-EXPORTER-HTTPS-PROXY: "https://someProxy"
  PROMETHEUS-EXPORTER-NO-PROXY: "https://someUrl"
---
# Source: sonarqube/templates/config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: duplicated-env-values.yaml-sonarqube-config
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
data:
  sonar.properties: |
---
# Source: sonarqube/templates/init-sysctl.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: duplicated-env-values.yaml-sonarqube-init-sysctl
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
data:
  init_sysctl.sh: |-
    set -o errexit
    set -o xtrace
    vmMaxMapCount=524288
    if [[ "$(sysctl -n vm.max_map_count)" -lt $vmMaxMapCount ]]; then
      sysctl -w vm.max_map_count=$vmMaxMapCount
      if [[ "$(sysctl -n vm.max_map_count)" -lt $vmMaxMapCount ]]; then
        echo "Failed to set initSysctl.vmMaxMapCount"; exit 1
      fi
    fi
    fsFileMax=131072
    if [[ "$(sysctl -n fs.file-max)" -lt $fsFileMax ]]; then
      sysctl -w fs.file-max=$fsFileMax
      if [[ "$(sysctl -n fs.file-max)" -lt $fsFileMax ]]; then
        echo "Failed to set initSysctl.fsFileMax"; exit 1
      fi
    fi
    nofile=131072
    if [[ "$(ulimit -n)" != "unlimited" ]]; then
      if [[ "$(ulimit -n)" -lt $nofile ]]; then
        ulimit -n $nofile
        if [[ "$(ulimit -n)" -lt $nofile ]]; then
          echo "Failed to set initSysctl.nofile"; exit 1
        fi
      fi
    fi
    nproc=8192
    if [[ "$(ulimit -u)" != "unlimited" ]]; then
      if [[ "$(ulimit -u)" -lt $nproc ]]; then
        ulimit -u $nproc
        if [[ "$(ulimit -u)" -lt $nproc ]]; then
          echo "Failed to set initSysctl.nproc"; exit 1
        fi
      fi
    fi
---
# Source: sonarqube/templates/install-plugins.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: duplicated-env-values.yaml-sonarqube-install-plugins
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
data:
  install_plugins.sh: |-
---
# Source: sonarqube/templates/prometheus-ce-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: duplicated-env-values.yaml-sonarqube-prometheus-ce-config
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
data:
  prometheus-ce-config.yaml: |-
    rules:
    - pattern: .*
---
# Source: sonarqube/templates/prometheus-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: duplicated-env-values.yaml-sonarqube-prometheus-config
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
data:
  prometheus-config.yaml: |-
    rules:
    - pattern: .*
---
# Source: sonarqube/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: duplicated-env-values.yaml-sonarqube
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: sonarqube
    release: duplicated-env-values.yaml
---
# Source: sonarqube/templates/sonarqube-sts.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: duplicated-env-values.yaml-sonarqube
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
    app.kubernetes.io/name: duplicated-env-values.yaml
    app.kubernetes.io/instance: duplicated-env-values.yaml
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: sonarqube
    app.kubernetes.io/component: duplicated-env-values.yaml-sonarqube
    app.kubernetes.io/version: "2025.6.0-enterprise"
spec:
  replicas: 1
  revisionHistoryLimit: 10
  serviceName: duplicated-env-values.yaml-sonarqube
  selector:
    matchLabels:
      app: sonarqube
      release: duplicated-env-values.yaml
  template:
    metadata:
      annotations:
        checksum/config: 3b1a1bb00fc970b903a147f76673940daea4a11096461f35d3fbf6b76933bdfe
        checksum/init-sysctl: 983bab4f775bef5c947ed29f9706da6ad6f96546a69fe95969bd34db07ad5985
        checksum/plugins: 8e1016d82a91e8031a3e022b9c81dbf2b55c16c6a405102aa108f3a5e185154a
        checksum/secret: c954f3e7aa4ee83bddaf155fb09494beac8057dd8f5ae0f4cc60b1d9921c74f7
        checksum/prometheus-config: 610b6602aa6f06beae9eb4394b2e26c9eb7fb30e253f144d4eefca3a6b9917d0
        checksum/prometheus-ce-config: 8b28d7e3ec17bd1e76d9b946ccc8c169c53c8570eefd2230b26257b49b084947
      labels:
        app: sonarqube
        release: duplicated-env-values.yaml
    spec:
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 0
      initContainers:
        - name: init-sysctl
          image: sonarqube:2025.6.0-enterprise
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
            readOnlyRootFilesystem: true
            runAsUser: 0
          command: ["/bin/bash", "-e", "/tmp/scripts/init_sysctl.sh"]
          volumeMounts:
            - name: init-sysctl
              mountPath: /tmp/scripts/
          env:
            - name: FOOBAR
              valueFrom:
                secretKeyRef:
                  key: BARFOO
                  name: sonarqube-ldap
            - name: SONAR_WEB_CONTEXT
              value: /some/context/
            - name: SONAR_WEB_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8000:/opt/sonarqube/conf/prometheus-config.yaml
                -Xmx1g
            - name: SONAR_CE_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8001:/opt/sonarqube/conf/prometheus-ce-config.yaml
                -Xms2G -Xmx2G -DsomeOption=some/Value
        - name: inject-prometheus-exporter
          image: sonarqube:2025.6.0-enterprise
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 0
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
          command: ["/bin/sh", "-c"]
          args: ["curl -s 'https://repo1.maven.org/maven2/io/prometheus/jmx/jmx_prometheus_javaagent/0.17.2/jmx_prometheus_javaagent-0.17.2.jar'  --output /data/jmx_prometheus_javaagent.jar -v"]
          volumeMounts:
            - mountPath: /data
              name: sonarqube
              subPath: data
          env:
            - name: http_proxy
              valueFrom:
                secretKeyRef:
                  name: duplicated-env-values.yaml-sonarqube-http-proxies
                  key: PROMETHEUS-EXPORTER-HTTP-PROXY
            - name: https_proxy
              valueFrom:
                secretKeyRef:
                  name: duplicated-env-values.yaml-sonarqube-http-proxies
                  key: PROMETHEUS-EXPORTER-HTTPS-PROXY
            - name: no_proxy
              valueFrom:
                secretKeyRef:
                  name: duplicated-env-values.yaml-sonarqube-http-proxies
                  key: PROMETHEUS-EXPORTER-NO-PROXY
            - name: FOOBAR
              valueFrom:
                secretKeyRef:
                  key: BARFOO
                  name: sonarqube-ldap
            - name: SONAR_WEB_CONTEXT
              value: /some/context/
            - name: SONAR_WEB_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8000:/opt/sonarqube/conf/prometheus-config.yaml
                -Xmx1g
            - name: SONAR_CE_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8001:/opt/sonarqube/conf/prometheus-ce-config.yaml
                -Xms2G -Xmx2G -DsomeOption=some/Value
      containers:
        - name: sonarqube
          image: sonarqube:2025.6.0-enterprise
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9000
              protocol: TCP
            - name: monitoring-web
              containerPort: 8000
              protocol: TCP
            - name: monitoring-ce
              containerPort: 8001
              protocol: TCP
          resources:
            limits:
              cpu: 800m
              ephemeral-storage: 512000M
              memory: 6144M
            requests:
              cpu: 400m
              ephemeral-storage: 1536M
              memory: 2048M
          env:
            - name: SONAR_HELM_CHART_VERSION
              value: 2026.1.0
            - name: SONAR_WEB_SYSTEMPASSCODE
              valueFrom:
                secretKeyRef:
                  name: duplicated-env-values.yaml-sonarqube-monitoring-passcode
                  key: SONAR_WEB_SYSTEMPASSCODE
            - name: FOOBAR
              valueFrom:
                secretKeyRef:
                  key: BARFOO
                  name: sonarqube-ldap
            - name: SONAR_WEB_CONTEXT
              value: /some/context/
            - name: SONAR_WEB_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8000:/opt/sonarqube/conf/prometheus-config.yaml
                -Xmx1g
            - name: SONAR_CE_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8001:/opt/sonarqube/conf/prometheus-ce-config.yaml
                -Xms2G -Xmx2G -DsomeOption=some/Value
          envFrom:
          livenessProbe:
            exec:
              command:
              - sh
              - -c
              - |
                wget --no-proxy --quiet -O /dev/null --timeout=1 --header="X-Sonar-Passcode: $SONAR_WEB_SYSTEMPASSCODE" "http://localhost:9000/some/context/api/system/liveness"
            failureThreshold: 6
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 1
          readinessProbe:
            exec:
              command:
              - sh
              - -c
              - |
                #!/bin/bash
                # A Sonarqube container is considered ready if the status is UP, DB_MIGRATION_NEEDED or DB_MIGRATION_RUNNING
                # status about migration are added to prevent the node to be kill while SonarQube is upgrading the database.
                if wget --no-proxy -qO- http://localhost:9000/some/context/api/system/status | grep -q -e '"status":"UP"' -e '"status":"DB_MIGRATION_NEEDED"' -e '"status":"DB_MIGRATION_RUNNING"'; then
                  exit 0
                fi
                exit 1
            failureThreshold: 6
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 1
          startupProbe:
            httpGet:
              scheme: HTTP
              path: /some/context/api/system/status
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 24
            timeoutSeconds: 1
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            runAsGroup: 0
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - mountPath: /opt/sonarqube/data
              name: sonarqube
              subPath: data
            - mountPath: /opt/sonarqube/temp
              name: sonarqube
              subPath: temp
            - mountPath: /opt/sonarqube/logs
              name: sonarqube
              subPath: logs
            - mountPath: /tmp
              name: tmp-dir
            - mountPath: /opt/sonarqube/extensions
              name: sonarqube
              subPath: extensions
            - mountPath: /opt/sonarqube/conf/prometheus-config.yaml
              subPath: prometheus-config.yaml
              name: prometheus-config
            - mountPath: /opt/sonarqube/conf/prometheus-ce-config.yaml
              subPath: prometheus-ce-config.yaml
              name: prometheus-ce-config
      serviceAccountName: default
      volumes:
        
        - name: init-sysctl
          configMap:
            name: duplicated-env-values.yaml-sonarqube-init-sysctl
            items:
              - key: init_sysctl.sh
                path: init_sysctl.sh
        - name: prometheus-config
          configMap:
            name: duplicated-env-values.yaml-sonarqube-prometheus-config
            items:
              - key: prometheus-config.yaml
                path: prometheus-config.yaml
        - name: prometheus-ce-config
          configMap:
            name: duplicated-env-values.yaml-sonarqube-prometheus-ce-config
            items:
              - key: prometheus-ce-config.yaml
                path: prometheus-ce-config.yaml
        - name: sonarqube
          emptyDir:
            {}
        - name : tmp-dir
          emptyDir:
            {}
---
# Source: sonarqube/templates/tests/sonarqube-test.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "duplicated-env-values.yaml-ui-test"
  annotations:
    "helm.sh/hook": test-success
    # Disable Istio sidecar injection for this test pod
    "sidecar.istio.io/inject": "false"
  labels:
    app: sonarqube
    chart: sonarqube-2026.1.0
    release: duplicated-env-values.yaml
    heritage: Helm
spec:
  automountServiceAccountToken: false
  containers:
    - name: duplicated-env-values.yaml-ui-test
      image: "sonarqube:2025.6.0-enterprise"
      imagePullPolicy: IfNotPresent
      command: ['wget']
      args: [
        '--retry-connrefused',
        '--waitretry=1',
        '--timeout=5',
        '-t',
        '12',
        '-qO-',
        'duplicated-env-values.yaml-sonarqube:9000/api/system/status'
        ]
      resources:
        limits:
          cpu: 500m
          ephemeral-storage: 1000M
          memory: 200M
        requests:
          cpu: 500m
          ephemeral-storage: 100M
          memory: 200M
  restartPolicy: Never
