---
# Source: sonarqube/templates/secret.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: proxies-values.yaml-sonarqube-monitoring-passcode
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
type: Opaque
data:
  SONAR_WEB_SYSTEMPASSCODE: "dGVzdA=="
---
# Source: sonarqube/templates/secret.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: proxies-values.yaml-sonarqube-http-proxies
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
type: Opaque
stringData:
  PLUGINS-HTTP-PROXY: "http://someProxy"
  PLUGINS-HTTPS-PROXY: "https://someProxy"
  PLUGINS-NO-PROXY: "https://someUrl"
  PROMETHEUS-EXPORTER-HTTP-PROXY: "http://someProxy"
  PROMETHEUS-EXPORTER-HTTPS-PROXY: "https://someProxy"
  PROMETHEUS-EXPORTER-NO-PROXY: "https://someUrl"
---
# Source: sonarqube/templates/config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: proxies-values.yaml-sonarqube-config
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
data:
  sonar.properties: |
---
# Source: sonarqube/templates/init-sysctl.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: proxies-values.yaml-sonarqube-init-sysctl
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
data:
  init_sysctl.sh: |-
    set -o errexit
    set -o xtrace
    vmMaxMapCount=524288
    if [[ "$(sysctl -n vm.max_map_count)" -lt $vmMaxMapCount ]]; then
      sysctl -w vm.max_map_count=$vmMaxMapCount
      if [[ "$(sysctl -n vm.max_map_count)" -lt $vmMaxMapCount ]]; then
        echo "Failed to set initSysctl.vmMaxMapCount"; exit 1
      fi
    fi
    fsFileMax=131072
    if [[ "$(sysctl -n fs.file-max)" -lt $fsFileMax ]]; then
      sysctl -w fs.file-max=$fsFileMax
      if [[ "$(sysctl -n fs.file-max)" -lt $fsFileMax ]]; then
        echo "Failed to set initSysctl.fsFileMax"; exit 1
      fi
    fi
    nofile=131072
    if [[ "$(ulimit -n)" != "unlimited" ]]; then
      if [[ "$(ulimit -n)" -lt $nofile ]]; then
        ulimit -n $nofile
        if [[ "$(ulimit -n)" -lt $nofile ]]; then
          echo "Failed to set initSysctl.nofile"; exit 1
        fi
      fi
    fi
    nproc=8192
    if [[ "$(ulimit -u)" != "unlimited" ]]; then
      if [[ "$(ulimit -u)" -lt $nproc ]]; then
        ulimit -u $nproc
        if [[ "$(ulimit -u)" -lt $nproc ]]; then
          echo "Failed to set initSysctl.nproc"; exit 1
        fi
      fi
    fi
---
# Source: sonarqube/templates/install-plugins.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: proxies-values.yaml-sonarqube-install-plugins
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
data:
  install_plugins.sh: |-
      rm -f /opt/sonarqube/extensions/plugins/*
      cd /opt/sonarqube/extensions/plugins
      curl   -fsSLO "https://github.com/sbaudoin/sonar-yaml/releases/download/v1.7.0/sonar-yaml-plugin-1.7.0.jar"
---
# Source: sonarqube/templates/prometheus-ce-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: proxies-values.yaml-sonarqube-prometheus-ce-config
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
data:
  prometheus-ce-config.yaml: |-
    rules:
    - pattern: .*
---
# Source: sonarqube/templates/prometheus-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: proxies-values.yaml-sonarqube-prometheus-config
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
data:
  prometheus-config.yaml: |-
    rules:
    - pattern: .*
---
# Source: sonarqube/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: proxies-values.yaml-sonarqube
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: sonarqube
    release: proxies-values.yaml
---
# Source: sonarqube/templates/sonarqube-sts.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: proxies-values.yaml-sonarqube
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
    app.kubernetes.io/name: proxies-values.yaml
    app.kubernetes.io/instance: proxies-values.yaml
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: sonarqube
    app.kubernetes.io/component: proxies-values.yaml-sonarqube
    app.kubernetes.io/version: "2026.1.0-enterprise"
spec:
  replicas: 1
  revisionHistoryLimit: 10
  serviceName: proxies-values.yaml-sonarqube
  selector:
    matchLabels:
      app: sonarqube
      release: proxies-values.yaml
  template:
    metadata:
      annotations:
        checksum/config: 92aef243d88684cf41743bc70fb8837d28c7f43a6d27458f16853d737343f021
        checksum/init-sysctl: 4edfd8804369316f4981d12c6e0b4c04939bb5ca10e59c6950ca3e359254937b
        checksum/plugins: c679b86b4c4017a91dd11df6fda8b946076317e4a77497bdb6004aa393ed75fa
        checksum/secret: cb23133aa979760ef36f9644281109d806c423e331cc4088afa73959e1eb45cb
        checksum/prometheus-config: 2d35934695f444e52351b8fa64c8721f9c2b7baa352bd7b30189553b45b611f7
        checksum/prometheus-ce-config: 7da62940b52c7df2326adc3bbaab0cba37ace36485c019e331d170f2934249c4
      labels:
        app: sonarqube
        release: proxies-values.yaml
    spec:
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 0
      initContainers:
        - name: init-sysctl
          image: sonarqube:2026.1.0-enterprise
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
            readOnlyRootFilesystem: true
            runAsUser: 0
          command: ["/bin/bash", "-e", "/tmp/scripts/init_sysctl.sh"]
          volumeMounts:
            - name: init-sysctl
              mountPath: /tmp/scripts/
          env:
            - name: SONAR_WEB_CONTEXT
              value: /
            - name: SONAR_WEB_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8000:/opt/sonarqube/conf/prometheus-config.yaml
            - name: SONAR_CE_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8001:/opt/sonarqube/conf/prometheus-ce-config.yaml
        - name: inject-prometheus-exporter
          image: sonarqube:2026.1.0-enterprise
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 0
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
          command: ["/bin/sh", "-c"]
          args: ["curl -s 'https://repo1.maven.org/maven2/io/prometheus/jmx/jmx_prometheus_javaagent/0.17.2/jmx_prometheus_javaagent-0.17.2.jar'  --output /data/jmx_prometheus_javaagent.jar -v"]
          volumeMounts:
            - mountPath: /data
              name: sonarqube
              subPath: data
          env:
            - name: http_proxy
              valueFrom:
                secretKeyRef:
                  name: proxies-values.yaml-sonarqube-http-proxies
                  key: PROMETHEUS-EXPORTER-HTTP-PROXY
            - name: https_proxy
              valueFrom:
                secretKeyRef:
                  name: proxies-values.yaml-sonarqube-http-proxies
                  key: PROMETHEUS-EXPORTER-HTTPS-PROXY
            - name: no_proxy
              valueFrom:
                secretKeyRef:
                  name: proxies-values.yaml-sonarqube-http-proxies
                  key: PROMETHEUS-EXPORTER-NO-PROXY
            - name: SONAR_WEB_CONTEXT
              value: /
            - name: SONAR_WEB_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8000:/opt/sonarqube/conf/prometheus-config.yaml
            - name: SONAR_CE_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8001:/opt/sonarqube/conf/prometheus-ce-config.yaml
        - name: install-plugins
          image: sonarqube:2026.1.0-enterprise
          imagePullPolicy: IfNotPresent
          command: ["sh", "-e", "/tmp/scripts/install_plugins.sh"]
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 0
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - mountPath: /opt/sonarqube/extensions/plugins
              name: sonarqube
              subPath: extensions/plugins
            - name: install-plugins
              mountPath: /tmp/scripts/
          env:
            - name: http_proxy
              valueFrom:
                secretKeyRef:
                  name: proxies-values.yaml-sonarqube-http-proxies
                  key: PLUGINS-HTTP-PROXY
            - name: https_proxy
              valueFrom:
                secretKeyRef:
                  name: proxies-values.yaml-sonarqube-http-proxies
                  key: PLUGINS-HTTPS-PROXY
            - name: no_proxy
              valueFrom:
                secretKeyRef:
                  name: proxies-values.yaml-sonarqube-http-proxies
                  key: PLUGINS-NO-PROXY
            - name: SONAR_WEB_CONTEXT
              value: /
            - name: SONAR_WEB_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8000:/opt/sonarqube/conf/prometheus-config.yaml
            - name: SONAR_CE_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8001:/opt/sonarqube/conf/prometheus-ce-config.yaml
      containers:
        - name: sonarqube
          image: sonarqube:2026.1.0-enterprise
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9000
              protocol: TCP
            - name: monitoring-web
              containerPort: 8000
              protocol: TCP
            - name: monitoring-ce
              containerPort: 8001
              protocol: TCP
          resources:
            limits:
              cpu: 800m
              ephemeral-storage: 512000M
              memory: 6144M
            requests:
              cpu: 400m
              ephemeral-storage: 1536M
              memory: 2048M
          env:
            - name: SONAR_HELM_CHART_VERSION
              value: 2026.2.0
            - name: SONAR_WEB_SYSTEMPASSCODE
              valueFrom:
                secretKeyRef:
                  name: proxies-values.yaml-sonarqube-monitoring-passcode
                  key: SONAR_WEB_SYSTEMPASSCODE
            - name: SONAR_WEB_CONTEXT
              value: /
            - name: SONAR_WEB_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8000:/opt/sonarqube/conf/prometheus-config.yaml
            - name: SONAR_CE_JAVAOPTS
              value: -javaagent:/opt/sonarqube/data/jmx_prometheus_javaagent.jar=8001:/opt/sonarqube/conf/prometheus-ce-config.yaml
          envFrom:
          livenessProbe:
            exec:
              command:
              - sh
              - -c
              - |
                wget --no-proxy --quiet -O /dev/null --timeout=1 --header="X-Sonar-Passcode: $SONAR_WEB_SYSTEMPASSCODE" "http://localhost:9000/api/system/liveness"
            failureThreshold: 6
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 1
          readinessProbe:
            exec:
              command:
              - sh
              - -c
              - |
                #!/bin/bash
                # A Sonarqube container is considered ready if the status is UP, DB_MIGRATION_NEEDED or DB_MIGRATION_RUNNING
                # status about migration are added to prevent the node to be kill while SonarQube is upgrading the database.
                if wget --no-proxy -qO- http://localhost:9000/api/system/status | grep -q -e '"status":"UP"' -e '"status":"DB_MIGRATION_NEEDED"' -e '"status":"DB_MIGRATION_RUNNING"'; then
                  exit 0
                fi
                exit 1
            failureThreshold: 6
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 1
          startupProbe:
            httpGet:
              scheme: HTTP
              path: /api/system/status
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 24
            timeoutSeconds: 1
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            runAsGroup: 0
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - mountPath: /opt/sonarqube/data
              name: sonarqube
              subPath: data
            - mountPath: /opt/sonarqube/temp
              name: sonarqube
              subPath: temp
            - mountPath: /opt/sonarqube/logs
              name: sonarqube
              subPath: logs
            - mountPath: /tmp
              name: tmp-dir
            - mountPath: /opt/sonarqube/extensions
              name: sonarqube
              subPath: extensions
            - mountPath: /opt/sonarqube/conf/prometheus-config.yaml
              subPath: prometheus-config.yaml
              name: prometheus-config
            - mountPath: /opt/sonarqube/conf/prometheus-ce-config.yaml
              subPath: prometheus-ce-config.yaml
              name: prometheus-ce-config
      serviceAccountName: default
      volumes:
        
        - name: init-sysctl
          configMap:
            name: proxies-values.yaml-sonarqube-init-sysctl
            items:
              - key: init_sysctl.sh
                path: init_sysctl.sh
        - name: install-plugins
          configMap:
            name: proxies-values.yaml-sonarqube-install-plugins
            items:
              - key: install_plugins.sh
                path: install_plugins.sh
        - name: prometheus-config
          configMap:
            name: proxies-values.yaml-sonarqube-prometheus-config
            items:
              - key: prometheus-config.yaml
                path: prometheus-config.yaml
        - name: prometheus-ce-config
          configMap:
            name: proxies-values.yaml-sonarqube-prometheus-ce-config
            items:
              - key: prometheus-ce-config.yaml
                path: prometheus-ce-config.yaml
        - name: sonarqube
          emptyDir:
            {}
        - name : tmp-dir
          emptyDir:
            {}
---
# Source: sonarqube/templates/tests/sonarqube-test.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "proxies-values.yaml-ui-test"
  annotations:
    "helm.sh/hook": test-success
    # Disable Istio sidecar injection for this test pod
    "sidecar.istio.io/inject": "false"
  labels:
    app: sonarqube
    chart: sonarqube-2026.2.0
    release: proxies-values.yaml
    heritage: Helm
spec:
  automountServiceAccountToken: false
  containers:
    - name: proxies-values.yaml-ui-test
      image: "sonarqube:2026.1.0-enterprise"
      imagePullPolicy: IfNotPresent
      command: ['wget']
      args: [
        '--retry-connrefused',
        '--waitretry=1',
        '--timeout=5',
        '-t',
        '12',
        '-qO-',
        'proxies-values.yaml-sonarqube:9000/api/system/status'
        ]
      resources:
        limits:
          cpu: 500m
          ephemeral-storage: 1000M
          memory: 200M
        requests:
          cpu: 500m
          ephemeral-storage: 100M
          memory: 200M
  restartPolicy: Never
