edition: enterprise

# SONAR-22466: Addresses the issues introduced by the refactoring of the templates
podLabels:
  azure.workload.identity/use: "true"

persistence:
  enabled: true

initFs:
  enabled: true

initContainers:
  resources:
    limits:
      cpu: 50m
      memory: 64Mi
